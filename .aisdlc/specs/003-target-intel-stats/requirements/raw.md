在监控目标列表页，每个监控目标卡片需要有一个可点击的入口，打开一个独立窗口（弹窗或侧边抽屉），显示该监控目标的详情统计数据：
1. 已监控信息总条数（所有 intels 记录数）
2. 有价值信息条数（is_noise = false 的记录数）
3. 无价值信息条数（is_noise = true 的记录数，如界面渲染变化、样式改变等噪音）

要求：
- 点击监控目标卡片上的"详情"或统计数字区域触发弹窗
- 弹窗内展示三项统计数据，样式清晰直观
- 数据从后端 API 实时获取（需要新增一个统计接口）
- 无价值信息可附带简要说明（如"界面渲染改变"、"样式调整"等噪音类型分布）
